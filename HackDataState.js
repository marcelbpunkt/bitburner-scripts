/**
 * Contains the names of all 4 states: before hack, after hack,
 * before grow, and after grow
 */
export const State = Object.freeze({
	BEFORE_HACK: "BEFORE_HACK",
	AFTER_HACK: "AFTER_HACK",
	BEFORE_GROW: "BEFORE_GROW",
	AFTER_GROW: "AFTER_GROW"
});